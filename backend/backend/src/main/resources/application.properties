spring.application.name= backend

# Configuración de la base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/parking_uja

spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Configuración del servidor
server.port=8080

# Configuración de sesiones
server.servlet.session.timeout=30m

# Configuración de seguridad
spring.security.user.name=admin
spring.security.user.password=admin

# Configuración de logging
logging.level.org.springframework=INFO
logging.level.com.parking.backend=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT Configuration
app.jwt-secret=parkingUjaSecretKey2024
app.jwt-expiration-milliseconds=86400000
# Dirección del broker MQTT (puede ser con ws://, tcp://, ssl://, etc.)
mqtt.broker.url=tcp://localhost:1883
# ID único del cliente (suscriptor)
mqtt.client.id.subscriber=admin

